# Legacy Manual Studio Archive

This archive preserves the markdown-first prototype that existed before the persistent multi-agent rebuild.

## Archived In Phase 0

- `studio_agents/` before the active package was renamed to `agents/`
- `scripts/run_orchestrator.py` and `scripts/test_openai.py` snapshots from the manual prototype
- misplaced or duplicate project documents captured before consolidation
