# Policy Review Inputs

This file exists so the weekly policy review can compare runtime governance behavior against human-maintained policy notes.

Current review focus:

- role-to-state permissions remain aligned with `governance/rules.yml`
- forbidden prompt patterns stay narrow and actionable
- task-state transitions stay inside the Kanban state machine
- Review -> Done requires at least two positive review votes
