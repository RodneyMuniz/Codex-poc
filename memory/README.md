# Memory

This directory stores lightweight framework memory snapshots used by health checks and session summaries.

- `framework_health.json` contains the latest health check result.
- `session_summaries.json` contains compact summaries of completed or blocked runs.
